/**
 * InTransitPanel — DEPRECATED
 *
 * Travel progress bars, "Finalizing arrival", TravelSession polling — all removed.
 * Characters now relocate instantly at scheduled time via processScheduledRelocations.
 * There is no in-transit state. This component renders nothing.
 */
export default function InTransitPanel() {
  return null;
}